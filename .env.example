# =============================================================================
# CONFIGURAÇÃO DE AMBIENTE - WILLIAM DISK PIZZA
# =============================================================================
# Este arquivo contém todas as variáveis de ambiente necessárias para executar
# a aplicação. Copie este arquivo para .env e preencha com os valores reais.
# NUNCA commite o arquivo .env com valores reais!

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Configurações do Supabase para autenticação e banco de dados
# Para desenvolvimento local, execute 'supabase start' e depois 'supabase status'
# para obter as URLs e chaves corretas

# URL pública do Supabase (visível no frontend)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321

# Chave anônima do Supabase (visível no frontend)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Chave de service role do Supabase (APENAS backend - NUNCA expor no frontend)
# Esta chave tem privilégios administrativos completos
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# URL de conexão direta com o banco PostgreSQL do Supabase
# Usado para operações que requerem acesso direto ao banco
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# =============================================================================
# JWT & AUTHENTICATION
# =============================================================================
# Chaves secretas para assinatura e verificação de tokens JWT
# IMPORTANTE: Use chaves diferentes e seguras em produção!

# Chave principal para tokens de acesso (mínimo 32 caracteres)
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long

# Chave para tokens de refresh (mínimo 32 caracteres)
JWT_REFRESH_SECRET=your-super-secret-refresh-token-with-at-least-32-characters

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Configurações gerais da aplicação

# URL base da aplicação
NEXTAUTH_URL=http://localhost:3000

# Chave secreta para NextAuth (se usado)
NEXTAUTH_SECRET=your-nextauth-secret

# Ambiente de execução (development, production, test)
NODE_ENV=development

# =============================================================================
# LOGGING & DEBUGGING
# =============================================================================
# Configurações para logs e depuração

# Habilitar logs de queries SQL (true/false)
ENABLE_QUERY_LOGS=true

# Habilitar logs de queries lentas (true/false)
ENABLE_SLOW_QUERY_LOGS=true

# Limite em ms para considerar uma query como lenta
SLOW_QUERY_THRESHOLD=1000

# =============================================================================
# SECURITY SETTINGS
# =============================================================================
# Configurações de segurança da aplicação

# Habilitar cookies seguros (true em produção)
SECURE_COOKIES=false

# Domínio para cookies (deixe vazio para localhost)
COOKIE_DOMAIN=

# =============================================================================
# RATE LIMITING
# =============================================================================
# Configurações para limitação de taxa de requisições

# Habilitar rate limiting (true/false)
ENABLE_RATE_LIMITING=true

# Janela de tempo para rate limiting em ms
RATE_LIMIT_WINDOW=60000

# Número máximo de requisições por janela
RATE_LIMIT_MAX=60

# =============================================================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# =============================================================================
# 
# 1. DESENVOLVIMENTO LOCAL:
#    - Execute 'supabase start' na raiz do projeto
#    - Execute 'supabase status' para obter as URLs e chaves
#    - Copie os valores para as variáveis SUPABASE acima
#
# 2. PRODUÇÃO:
#    - Crie um projeto no Supabase.com
#    - Obtenha as chaves no dashboard do projeto
#    - Configure todas as variáveis com valores de produção
#    - Use chaves JWT diferentes e seguras
#    - Configure NODE_ENV=production
#
# 3. SEGURANÇA:
#    - NUNCA commite o arquivo .env
#    - Use chaves diferentes para cada ambiente
#    - Mantenha as chaves de service role seguras
#    - Configure HTTPS em produção
#
# =============================================================================